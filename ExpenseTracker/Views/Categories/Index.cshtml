@model IEnumerable<ExpenseTracker.Models.Categories>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
 
     <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categoryexpenselimit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categoryexpenselimit)
            </td>
            <td>
                <a class="btn btn-secondary" asp-action="Edit" asp-route-id="@item.CategoryId">Edit</a>
                <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.CategoryId">Details</a>
                <a class="btn btn-warning"
                onclick="return confirm(&quot; are you sure to delete this record?&quot;)"
                asp-action="Delete"
                asp-route-id="@item.CategoryId">Delete</a>

            </td>
        </tr>
}
    </tbody>
</table>
